<% flash.each do | message_type, message_content | %>
  <div class="ui message <%= message_type %> transition">
    <i onclick="this.parentElement.style.display='none';" class="close icon"></i>
    <div class="header">
      <%= message_content %>
    </div>
  </div>
<% end %>